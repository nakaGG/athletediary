<%= link_to 'イベント追加', new_event_path, class: :form__btn %>
<%= month_calendar events: @events do |date, events| %>
  <%= date.day %>
    <% events.each do |event| %>
      <div>
        <%= event.title %>
      </div>
    <% end %>
<% end %>